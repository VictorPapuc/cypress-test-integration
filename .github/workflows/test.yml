name: First Workflow

on: [ push ]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo
        run: echo "Hello World"
      - name: multiline sript
        run: |
          node -v
          npm -v